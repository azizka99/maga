version: "3.9"

services:
  db:
    image: mysql:8.0
    ports:
      - 3306:3306
    environment:
      - MYSQL_ROOT_PASSWORD=maga2001
      - MYSQL_USER=magaRoot
      - MYSQL_PASSWORD=maga2001
      - MYSQL_DATABASE=maga_db
    restart: always
